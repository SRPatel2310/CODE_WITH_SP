// create a function to calculate the area  of a reactangle  given  its  width and hight .

let areaofreactangle = (width, hight) => {
  return width * hight;
};

let area = areaofreactangle(10, 5);
console.log("Area of Reactangle is:" + area);
